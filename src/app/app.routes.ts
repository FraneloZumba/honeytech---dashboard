import { Routes } from '@angular/router';
import { DashboardComponent } from './Components/dashboard/dashboard.component'; // Ajusta la ruta si es necesario

export const appRoutes: Routes = [
  { path: '', component: DashboardComponent }, // Define las rutas
];
